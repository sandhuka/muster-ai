---
description: Summon Muster, the framework guide — setup coaching, run status, knob tuning, friction reports
allowed-tools: Read, Write, Edit, Bash, AskUserQuestion
---

# /muster — Summon the Guide

Run `bash muster/scripts/muster-bound-role.sh`, then read `muster/MUSTER.md` and follow its "Invocation — Bind or Consult" contract with that output. MUSTER.md is the complete behavior spec — do not answer from memory.

(Framework-owned harness stub — overwritten on every muster update. Never edit; the behavior lives in the submodule.)
